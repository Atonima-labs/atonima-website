import { AnimatePresence, motion } from 'framer-motion';
import { Check, X } from 'lucide-react';
import { FormEvent, useEffect, useState } from 'react';
type Props={open:boolean;onClose:()=>void};
export default function Modal({open,onClose}:Props){const [sent,setSent]=useState(false);useEffect(()=>{if(!open)setSent(false)},[open]);const submit=(e:FormEvent)=>{e.preventDefault();setSent(true)};return <AnimatePresence>{open&&<div className="fixed inset-0 z-[100] grid place-items-center p-4" role="dialog" aria-modal="true" aria-labelledby="modal-title"><motion.button aria-label="Close dialog" onClick={onClose} className="absolute inset-0 bg-slate-950/50 backdrop-blur-sm" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/><motion.div initial={{opacity:0,y:20,scale:.97}} animate={{opacity:1,y:0,scale:1}} exit={{opacity:0,y:10,scale:.97}} className="card relative w-full max-w-lg overflow-hidden shadow-2xl"><header className="flex items-start justify-between border-b p-6 sm:p-8"><div><h2 id="modal-title" className="text-xl font-bold">Request Early Access</h2><p className="mt-1 text-sm text-slate-500">Join the Atonima Control early-access program.</p></div><button onClick={onClose} aria-label="Close"><X className="size-5"/></button></header>{sent?<div className="p-10 text-center"><div className="mx-auto mb-5 grid size-14 place-items-center rounded-full bg-teal-50"><Check className="text-mint"/></div><h3 className="text-lg font-bold">Request received.</h3><p className="mt-2 text-sm text-slate-500">Thank you. We’ll be in touch with early access details soon.</p><button onClick={onClose} className="btn-secondary mt-6">Close</button></div>:<form onSubmit={submit} className="space-y-5 p-6 sm:p-8"><div className="grid gap-4 sm:grid-cols-2"><Field label="Name" type="text" placeholder="Your name"/><Field label="Email" type="email" placeholder="you@company.com"/></div><Field label="Company" type="text" placeholder="Company or project name" optional/><label className="block text-xs font-semibold">Role<select className="mt-2 w-full rounded-lg border border-slate-200 bg-canvas px-3 py-3 text-sm" defaultValue=""><option value="" disabled>Select your role</option><option>Developer</option><option>Founder</option><option>Investor</option><option>Enterprise</option><option>Partner</option></select></label><button className="btn-primary w-full" type="submit">Request Early Access</button></form>}</motion.div></div>}</AnimatePresence>}
function Field({label,type,placeholder,optional=false}:{label:string;type:string;placeholder:string;optional?:boolean}){return <label className="block text-xs font-semibold">{label}{optional&&<span className="font-normal text-slate-400"> (optional)</span>}<input required={!optional} type={type} placeholder={placeholder} className="mt-2 w-full rounded-lg border border-slate-200 bg-canvas px-3 py-3 text-sm"/></label>}
